# name: 2-minute-cron
# on:
#   schedule:
#     - cron: '*/10 * * * *'
# jobs:
#   cron:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Call our API route
#         run: |
#           curl --request GET --url 'https://news-something.vercel.app/api/feed/read'